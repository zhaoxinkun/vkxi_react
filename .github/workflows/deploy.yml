name: Deploy React Admin

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 拉代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 安装 Node
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3. 启用 pnpm（官方推荐）
      - name: Enable corepack (pnpm)
        run: |
          corepack enable
          corepack prepare pnpm@8 --activate

      - name: Verify pnpm
        run: pnpm -v

      # 4. 安装依赖（⚠️ 一定要在项目目录）
      - name: Install dependencies
        working-directory: higschool_react
        run: pnpm install --frozen-lockfile

      # 5. 构建
      - name: Build
        working-directory: higschool_react
        run: pnpm build

      # 6. 打包 dist
      - name: Archive dist
        working-directory: higschool_react
        run: tar -czf dist.tar.gz dist

      # 7. 上传到服务器
      - name: Upload dist to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          source: higschool_react/dist.tar.gz
          target: /tmp

      # 8. 发布 + 切软链接
      - name: Publish on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            set -e

            APP_DIR="/www/vixi_react_admin"
            RELEASE_DIR="$APP_DIR/releases/$(date +%Y%m%d%H%M%S)"

            mkdir -p "$RELEASE_DIR"
            tar -xzf /tmp/dist.tar.gz -C "$RELEASE_DIR"

            ln -sfn "$RELEASE_DIR/dist" "$APP_DIR/current"

            rm -f /tmp/dist.tar.gz

            cd "$APP_DIR/releases"
            ls -1dt */ | tail -n +6 | xargs -r rm -rf
